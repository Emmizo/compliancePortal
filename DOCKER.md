# Docker

All steps to install dependencies, run **Docker Compose**, use URLs, volumes, commands, troubleshooting, and the **non-Docker** path are in **[README.md](README.md)** under **How I run it**.

This file is only here so older links to `DOCKER.md` still resolve.
